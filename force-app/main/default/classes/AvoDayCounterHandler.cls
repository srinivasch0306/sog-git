//Author by Jason li

public class AvoDayCounterHandler {
    public static void dayMethod() {
        Integer dayCounter = 0;
       
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
                dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
                dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
                dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
                dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
                dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
                dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
            dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        dayCounter++; dayCounter++; dayCounter++; dayCounter++; dayCounter++;
        
    } 

}